const rollno = document.getElementById('rollno');
const submit = document.getElementById('submit');

submit.addEventListener('click',generatelist);

function generatelist(e)
{
    console.log(rollno.value);
    var arr=[];
    
}